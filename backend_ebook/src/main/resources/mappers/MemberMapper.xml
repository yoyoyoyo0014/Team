<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.ebook.dao.MemberDAO">

	<select id="findMemberById" parameterType="string" resultType="MemberVO">
		
		select * from member
			where me_id = #{me_id}
		
	</select>
   
   <select id="selectMemberByNaverId" parameterType="String" resultType="MemberVO">
   		select * from member
   			where me_naverId = #{naverId}
   </select>
   
   <select id="findMemberByEmail" resultType="MemberVO">
   	select * from member where me_email = #{email}
   </select>
   
   <insert id="saveMember">
    insert into member (
        me_id, me_nickname, me_pw, me_email, me_address,
        me_phone, me_birth,
        
    ) values (
        #{me_id}, #{me_nickname}, #{me_pw}, null, #{me_address},
        #{me_phone}, #{me_birth},
        
    )
    
</insert>
   
</mapper>